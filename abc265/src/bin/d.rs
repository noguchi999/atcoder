use proconio::input;

fn main() {
    input! {
        n: usize,
        p: usize,
        q: usize,
        r: usize
    };
    input! {
        A: [i32],
    };

    let mut p_list: Vec<usize> = Vec::new();
    let mut S = A[0];
}
